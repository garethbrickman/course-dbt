version: 2

macros:
  - name: lbs_to_kgs
    description: convert lbs to kgs
  - name: positive_values
    description: check if numeric values in column are +ve 
  - name: make_event_types_with_counts
    description: creates count case when statements for each event type

